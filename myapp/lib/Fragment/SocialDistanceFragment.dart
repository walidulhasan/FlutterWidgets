import 'package:flutter/material.dart';

class SocialDistanceFragment extends StatelessWidget {
  const SocialDistanceFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("social_distance"));
  }
}